# 45m (built-in library)

This folder represents the built-in `45m` core library.

In this Python-powered prototype, the actual implementation lives in `libs/_45m/` and is automatically loaded by the interpreter.

